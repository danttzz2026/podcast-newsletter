# Secrets
.env

# Runtime state — daemon-only (machine-specific). state.json is committed
# so GitHub Actions has memory across runs.
daemon_state.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# macOS
.DS_Store
