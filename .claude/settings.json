{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -q '^/Users/dantebattista/podcast-newsletter/' && git -C /Users/dantebattista/podcast-newsletter pull || true",
            "statusMessage": "Pulling latest from GitHub..."
          }
        ]
      }
    ]
  }
}
